language: python
python:
  - "2.6"
  - "2.7"
# command to install dependencies
install:
  - pip install .
  - pip install -r requirements.txt
# command to run tests
script: scrapy crawl test_spider

# this is not working, will fix CI with org repo later...